fulltractor_dbt:
  target: ci
  outputs:
    ci:
      type: postgres
      host: localhost
      port: 5433
      user: dbt_user
      password: "{{ env_var('POSTGRES_PASSWORD') }}"
      dbname: fulltractor
      schema: analytics
      threads: 4